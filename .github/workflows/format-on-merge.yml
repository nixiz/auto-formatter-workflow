name: Run Python Script and Format Code on Pull Request Merge

on:
  pull_request:
    types: [closed]
     #branches:
      # - 'feature-*'
       #- master

jobs:
  run_script:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'
      #- name: Install dependencies
      #  run: pip install -r requirements.txt
      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v35
      - name: List files
        id: list_files
        run: |
          git fetch origin ${GITHUB_BASE_REF}
          #git checkout ${GITHUB_BASE_REF}
          head=$(git rev-parse ${GITHUB_BASE_REF})
          echo $head
          echo $(git diff --name-only $head)
          echo "::set-output name=files::$(git diff --name-only $head)"
      - name: Run Python script
        run: python ./scripts/script.py ${{ steps.list_files.outputs.files }}
      - name: Run Python script - 2
        run: python ./scripts/script.py ${{ steps.changed-files.outputs.all_changed_files }}
      # - name: Commit changes
      #   run: |
      #     git config --global user.email "github-actions@github.com"
      #     git config --global user.name "GitHub Actions"
      #     git commit -a -m "Format code with clang-format"
      #     git push origin HEAD:${{ github.event.pull_request.head.ref }}
