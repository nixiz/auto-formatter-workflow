name: Run Python Script and Format Code on Pull Request Merge

on:
  pull_request:
    types: [closed]
     #branches:
      # - 'feature-*'
       #- master

jobs:
  run_script:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 2
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'
      #- name: Install dependencies
      #  run: pip install -r requirements.txt
      - name: List files
        id: list_files
        run: |
          git fetch origin ${GITHUB_REF}
          git checkout ${GITHUB_SHA}
          echo ::set-output name=files::$(git diff --name-only ${GITHUB_BASE_REF} ${GITHUB_HEAD_REF})
      - name: Run Python script
        run: python ./scripts/script.py ${{ steps.list_files.outputs.files }}
      # - name: Commit changes
      #   run: |
      #     git config --global user.email "github-actions@github.com"
      #     git config --global user.name "GitHub Actions"
      #     git commit -a -m "Format code with clang-format"
      #     git push origin HEAD:${{ github.event.pull_request.head.ref }}
